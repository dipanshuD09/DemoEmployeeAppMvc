Write the code for the below scenario: Create table named Employees with columns (Empid, EmpName, Gender, DOB,Place(State),Photo). 
For the above requirement, in User interface use Text boxes for EmpId & EmpName,Textbox with 
calendar for DOB, Radio Button for Gender,Place (Dropdown), File Upload Control for Image. Perform CRUD operations. 
After Saving the data, represent the data using GridView Control and perform row wise Updation and Deletion within the GridView control 
using Same Controls.

// With Bulk Import
